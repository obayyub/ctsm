<% content_for :body do %>
  <ul id="publications">
    <% @publications.each do |publication| %>
      <li>
        <%= link_to publication.category, refinery.publications_publication_path(publication) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
